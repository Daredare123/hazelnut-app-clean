@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Localization
@using HazelnutVeb

@inject IStringLocalizer<SharedResource> Localizer

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - HazelnutVeb</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#0d6efd">
    <style>
        body { overflow-x: hidden; }
        #sidebar-wrapper { min-height: 100vh; margin-left: -15rem; transition: margin .25s ease-out; }
        #sidebar-wrapper .sidebar-heading { padding: 0.875rem 1.25rem; font-size: 1.2rem; }
        #sidebar-wrapper .list-group { width: 15rem; }
        #page-content-wrapper { min-width: 100vw; }
        body.sb-sidenav-toggled #sidebar-wrapper { margin-left: 0; }
        @@media (min-width: 768px) {
            #sidebar-wrapper { margin-left: 0; }
            #page-content-wrapper { min-width: 0; width: 100%; }
            body.sb-sidenav-toggled #sidebar-wrapper { margin-left: -15rem; }
        }
        .list-group-item-action { border: none; padding: 12px 20px; }
        .list-group-item-action:hover { background-color: #495057; color: #fff !important; }
        .list-group-item.active { background-color: #0d6efd; color: #fff; border-color: #0d6efd; }
        .dark-mode { background-color: #121212; color: #ffffff; }
        .dark-mode .card { background-color: #1e1e1e; }
        .dark-mode .table { color: #ffffff; }
        .dark-mode .form-control { background-color: #2a2a2a; color: #ffffff; border-color: #444; }
        .dark-mode .navbar { background-color: #1a1a1a !important; }
    </style>
</head>
<body class="bg-light">
    <div class="d-flex" id="wrapper">
        <!-- Sidebar -->
        @if (User.Identity.IsAuthenticated)
        {
            <div class="bg-dark border-end" id="sidebar-wrapper">
                <div class="sidebar-heading border-bottom bg-dark text-white fw-bold text-uppercase text-center py-4">
                    <i class="bi bi-nut-fill text-warning me-2"></i>HazelnutVeb
                </div>
                <div class="list-group list-group-flush">
                    @if (User.IsInRole("Admin"))
                    {
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Home" asp-action="Dashboard">
                            <i class="bi bi-speedometer2 me-2"></i> @Localizer["Dashboard"]
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Sales" asp-action="Index">
                            <i class="bi bi-cart3 me-2"></i> @Localizer["Sales"]
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Reservations" asp-action="Index">
                            <i class="bi bi-clock-history me-2"></i> @Localizer["Reservations"]
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Expenses" asp-action="Index">
                            <i class="bi bi-wallet2 me-2"></i> @Localizer["Expenses"]
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Inventory" asp-action="Index">
                            <i class="bi bi-box-seam me-2"></i> @Localizer["Inventory"]
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Reports" asp-action="Monthly">
                            <i class="bi bi-graph-up-arrow me-2"></i> @Localizer["Reports"]
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Clients" asp-action="Index">
                            <i class="bi bi-people me-2"></i> @Localizer["Clients"]
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Users" asp-action="Index">
                            <i class="bi bi-person-lines-fill me-2"></i> @Localizer["Users"]
                        </a>
                    }
                    else if (User.IsInRole("Client"))
                    {
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Reservations" asp-action="MyOrders">
                            <i class="bi bi-house-door me-2"></i> Dashboard
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Reservations" asp-action="Index">
                            <i class="bi bi-clock-history me-2"></i> My Reservations
                        </a>
                        <a class="list-group-item list-group-item-action list-group-item-dark bg-dark text-white-50" asp-controller="Reservations" asp-action="Create">
                            <i class="bi bi-plus-circle me-2"></i> Create Reservation
                        </a>
                    }
                </div>
                <div class="mt-auto p-3 border-top border-secondary">
                    <a class="btn btn-outline-danger w-100" asp-controller="Account" asp-action="Logout">
                        <i class="bi bi-box-arrow-right me-2"></i> @Localizer["Logout"]
                    </a>
                </div>
            </div>
        }

        <!-- Page Content -->
        <div id="page-content-wrapper">
             @if (User.Identity.IsAuthenticated)
             {
                <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm px-4">
                    <button class="btn btn-light" id="sidebarToggle"><i class="bi bi-list fs-4"></i></button>
                    <div class="ms-auto d-flex align-items-center">
                        <div class="me-3">
                            @{
                                var currentCulture = System.Threading.Thread.CurrentThread.CurrentUICulture.Name;
                            }
                            <div class="btn-group btn-group-sm" role="group">
                                <a href="?culture=mk" class="btn @(currentCulture == "mk" ? "btn-primary" : "btn-outline-primary")">MK</a>
                                <a href="?culture=en" class="btn @(currentCulture == "en" ? "btn-primary" : "btn-outline-primary")">EN</a>
                            </div>
                        </div>
                        <button class="btn btn-outline-secondary border-0 me-3 dark-mode-toggle" id="darkModeToggle">
                            🌙
                        </button>
                        <div class="dropdown">
                            <a class="nav-link dropdown-toggle text-dark fw-bold" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-person-circle me-1"></i> @User.Identity.Name
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end shadow">
                                <li><a class="dropdown-item" href="#">@Localizer["Profile"]</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" asp-controller="Account" asp-action="Logout">@Localizer["Logout"]</a></li>
                            </ul>
                        </div>
                    </div>
                </nav>
             }
             else
             {
                 <!-- Simple Navbar for non-authenticated users (Login page) -->
                 <nav class="navbar navbar-light bg-white border-bottom px-4 shadow-sm d-flex justify-content-between">
                     <span class="navbar-brand mb-0 h1"><i class="bi bi-nut-fill text-warning me-2"></i>HazelnutVeb</span>
                     <button class="btn btn-outline-secondary border-0 dark-mode-toggle" id="darkModeToggleGuest">
                         🌙
                     </button>
                 </nav>
             }

            <div class="container-fluid p-4">
                @RenderBody()
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', event => {
            const sidebarToggle = document.body.querySelector('#sidebarToggle');
            if (sidebarToggle) {
                sidebarToggle.addEventListener('click', event => {
                    event.preventDefault();
                    document.body.classList.toggle('sb-sidenav-toggled');
                });
            }

            const isDark = localStorage.getItem("theme") === "dark";
            if (isDark) {
                document.body.classList.add("dark-mode");
                document.body.classList.remove("bg-light");
            }

            const toggles = document.querySelectorAll('.dark-mode-toggle');
            toggles.forEach(toggle => {
                toggle.innerHTML = isDark ? '☀️' : '🌙';
                toggle.addEventListener('click', () => {
                    document.body.classList.toggle('dark-mode');
                    const currentlyDark = document.body.classList.contains('dark-mode');
                    
                    if (currentlyDark) {
                        localStorage.setItem("theme", "dark");
                        document.body.classList.remove("bg-light");
                        toggles.forEach(t => t.innerHTML = '☀️');
                    } else {
                        localStorage.setItem("theme", "light");
                        document.body.classList.add("bg-light");
                        toggles.forEach(t => t.innerHTML = '🌙');
                    }
                });
            });
        });

        if ('serviceWorker' in navigator) {
            navigator.serviceWorker.register('/sw.js')
                .then(() => console.log("SW registered"));
        }
    </script>
    @if (User.Identity.IsAuthenticated)
    {
        <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-app.js"></script>
        <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-messaging.js"></script>
        <script>
            const firebaseConfig = {
                apiKey: "AIzaSyDfhE0OubxHF-EEN_LkRm1Nm79gLraRM0Q",
                authDomain: "hazelnutarena-e2297.firebaseapp.com",
                projectId: "hazelnutarena-e2297",
                storageBucket: "hazelnutarena-e2297.firebasestorage.app",
                messagingSenderId: "279698258773",
                appId: "1:279698258773:web:010cdbdbbe201ecde78ddc"
            };
            if (!firebase.apps.length) {
                firebase.initializeApp(firebaseConfig);
            }
            // Manual opt-in notification toggle required for iOS!
            window.enableNotifications = async function () {
                const permission = await Notification.requestPermission();
                
                if (permission === 'granted') {
                    console.log("Notifications allowed");
                    alert("Notifications enabled ✅");
                    
                    messaging.getToken({ vapidKey: "BI3HfXHngb4xF6vb7pgjqEjjN0FW2k-ilt6d7GtgD5lq7rX-5YhLpT5bt2PLXjfGQAqiJ5Gq7Q8tmcstRC_8PKo" }).then((currentToken) => {
                        if (currentToken) {
                            fetch('/Notification/SaveToken', {
                                method: 'POST',
                                headers: { 'Content-Type': 'application/json' },
                                body: JSON.stringify({ token: currentToken })
                            }).then(response => {
                                console.log('Firebase token synced.');
                            }).catch(err => console.error('Error saving token', err));
                        }
                    }).catch(err => console.log('Error getting token', err));
                } else {
                    console.log("Notifications denied");
                    alert("Notifications denied ❌");
                }
            };

            messaging.onMessage((payload) => {
                console.log('Foreground message received. ', payload);
            });
        </script>
    }
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
